{
  "name": "@workspace/backend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "prepare": "nitro prepare",
    "preview": "node .output/server/index.mjs",
    "typecheck": "tsc --noEmit",
    "db": "drizzle-kit",
    "auth:generate": "pnpm dlx @better-auth/cli@latest generate --config ./src/lib/auth.ts --y --output ./src/lib/schema/auth.schema.ts && prettier --write ./src/lib/schema/auth.schema.ts"
  },
  "exports": {
    "./trpc": "./src/lib/trpc.ts",
    "./router": "./src/lib/trpc-router.ts"
  },
  "devDependencies": {
    "nitropack": "latest"
  },
  "dependencies": {
    "@falcondev-oss/nitro-trpc-event-handler": "^0.1.1",
    "@trpc/server": "^11.1.0",
    "superjson": "^2.2.2"
  }
}
